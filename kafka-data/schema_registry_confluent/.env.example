# Schema Registry Secrets
# Copy this file to .env and fill in the actual values

# Metadata encoder secret (base64 encoded 32-byte key)
# Generate new key: openssl rand -base64 32
SCHEMA_REGISTRY_METADATA_ENCODER_SECRET=YOUR_SECRET_HERE
