# Debezium Config: Postgres Local → Kafka
# Source: Postgres Local (127.0.0.1)
# Tables: customers, orders, order_items
# Publication: migration_pub

# 1. SINK - Send to Kafka
debezium.sink.type=kafka
debezium.sink.kafka.producer.bootstrap.servers=localhost:9092
debezium.sink.kafka.producer.key.serializer=org.apache.kafka.common.serialization.StringSerializer
debezium.sink.kafka.producer.value.serializer=org.apache.kafka.common.serialization.StringSerializer

# 2. FORMAT
debezium.format.key=json
debezium.format.value=json

# 3. SOURCE - Postgres Local
debezium.source.connector.class=io.debezium.connector.postgresql.PostgresConnector
debezium.source.plugin.name=pgoutput
debezium.source.database.hostname=127.0.0.1
debezium.source.database.port=5432
debezium.source.database.user=fastapi_user
debezium.source.database.password=${DB_PASSWORD}
debezium.source.database.dbname=fastapi_db
debezium.source.database.sslmode=require

# 4. PUBLICATION & REPLICATION
debezium.source.publication.name=migration_pub
debezium.source.slot.name=debezium_pg_local

# 5. FILTER - Migration tables only
debezium.source.topic.prefix=pg_local
debezium.source.schema.include.list=public
debezium.source.table.include.list=public.customers,public.orders,public.order_items

# 6. SNAPSHOT MODE
# initial = snapshot existing data (587 customers) then stream new changes
# never = skip snapshot, only stream new changes after Debezium starts
debezium.source.snapshot.mode=initial

# 7. OFFSET STORAGE
debezium.source.offset.storage.file.filename=data/offsets-pg-local.dat
debezium.source.offset.flush.interval.ms=1000

# 8. TRANSFORMS - Add metadata
debezium.transforms=addTimestamp
debezium.transforms.addTimestamp.type=org.apache.kafka.connect.transforms.InsertField$Value
debezium.transforms.addTimestamp.timestamp.field=_migration_ts
